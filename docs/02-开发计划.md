# cc-remote 开发计划（2026-09-02 定稿）

> 总工期 ≈ 4 天。M0 是生死验证，必须最先做；M1 可并行。前置事实依据见 `03-事实调研汇编.md`。

## 0. 前置环境核对

```bash
# WSL 里
docker --version          # 期望 Docker 29（已装）
node -v                   # cc-connect npm 安装需要（或直接用 GitHub release 二进制）
opencode --version        # 宿主机凭证在 ~/.config/opencode 与 ~/.local/share/opencode
```

## 1. 里程碑表

| 里程碑 | 任务 | 验收标准 | 产出 |
|---|---|---|---|
| **M0 · ACP 链路 Spike**（0.5d，**最先做，失败即触发兜底评估**） | WSL 裸跑 cc-connect 二进制 + `opencode acp` 手动握手；验证：①ACP `session/new` 出模型列表（#31076 风险点）②触发跨目录 ask（external_directory）③审批卡往返 | ①模型列表正常 ②权限卡到飞书、点批准任务继续 ③点拒绝任务中止 | 结论记录：走 ACP / 降级兜底 B / C |
| **M1 · QQ 机器人**（0.5d，与 M0 并行） | ~~飞书~~（2026-09-02 变更为 QQ，见 01 文档第 0 节）：`q.qq.com` 个人开发者实名注册 → 创建机器人 → 拿 AppID/AppSecret → 沙箱加自己为测试人员 → cc-connect `qqbot` 平台接线实测（WS 网关/单聊收发/审批按钮） | 沙箱里和机器人单聊：发 hi 得 agent 回复；权限请求出【允许/拒绝/允许所有】键盘按钮且点击生效 | AppID/AppSecret（入配置，勿进 git） |
| **M2 · 容器化**（1d） | Dockerfile：cc-connect（锁最新 release，含 #1746）+ opencode CLI v1.18.26；compose：挂载 config / `~/.cc-connect`(data_dir) / `~/.config/opencode` + `~/.local/share/opencode`（凭证）/ `~/AgentRoot` | 容器 up → 飞书机器人在线 → 发 "hi" 得到 agent 回复 | 镜像 + compose + 配置模板（AppID/Secret 走 .env） |
| **M3 · 工作区与权限**（0.5d） | 建 `~/AgentRoot` + 首个项目；opencode.json 权限（根内 allow / 跨出 ask / 危险 deny）；`[[projects]]` 登记 work_dir；`allow_from` 白名单 | 根内任务直跑；跨目录任务出审批卡；非白名单消息无响应 | AgentRoot + 权限配置 + 配置文档初稿 |
| **M4 · 会话/队列/互动验证**（0.5d） | 验证 /new /list /switch、30min 空闲、FIFO（连发 3 条）、/compress、skills/subagent 调用 | 全部行为符合预期；/stop 禁用已记入文档 | 飞书侧使用手册（命令表） |
| **M5 · Dashboard 与自愈**（0.5d） | :9820 映射 + 管理鉴权；`restart:always` + WSL systemd 开机自启 docker；**宿主** systemd timer 每日重启容器（治 ACP 泄漏 #38064 + 规避 Docker cron bug #1719）；日志轮转 | 重启 WSL 全链路自愈；手机同 WiFi 可开 Dashboard | compose 终版 + 运维手册 |
| **M6 · 收尾**（0.5d） | 文档成套（权限/yolo 开关/FAQ/已知 bug）；提 issue：cc-connect 催 LICENSE、关注 #1100/#1776；记录版本锁定 | 从零重建按文档一次通过 | 全套文档 + issue 记录 |

## 2. 风险对策表

| 风险 | 等级 | 对策 |
|---|---|---|
| opencode ACP `session/new` "No models available"（opencode #31076 关为 NOT_PLANNED，修复 PR 未合并） | 🔴 | M0 Day-1 用 v1.18.26 实测；不通→兜底 B |
| cc-connect #1776：external_directory 审批被硬拒 + /stop 清会话（已定位未发版） | 🔴 | M0 实测 ACP 路径是否复现；/stop 禁用；跟踪发版 |
| ACP 空会话泄漏（opencode #38064） | 🟡 | 每日定时重启容器 |
| acp 进程风暴（opencode #40696，客户端异常拉起 15-22 个进程） | 🟡 | 锁版本 + 监控进程数 + 重启策略 |
| Docker 内 cron 找不到 daemon（cc-connect #1719，修复 #1720 未合并） | 🟡 | 不用内建 cron，宿主 systemd timer |
| 多 app_id 双连接修复挂起（cc-connect #1563） | 🟡 | 只影响 v2 多机，跟踪 PR |
| opencode agent 只回一次（cc-connect #713，stale） | 🟢 | 走 ACP agent 理论不适用，M4 观测 |
| 飞书流式卡片需客户端 ≥7.20 | 🟢 | cc-connect 有增量刷卡兜底，不阻塞 |
| cc-connect 单人维护、PR 合并慢 | 🟢 | 锁版本；关键 bug 有源码可自修 |

## 3. 版本锁定（写进 compose/.env，升级需用户同意）

- opencode：**v1.18.26**
- cc-connect：最新 GitHub release（v1.5.0+，须含 #1746 飞书大文件修复——**注：#1746 于 2026-08-27 合并，晚于 v1.5.0 release，M2 从 main@b39c11f 源码编译，spike 已验证可编**）；不用内建 cron
- QQ 机器人：个人开发者，沙箱先行；`qqbot` 平台（官方 WS 网关）

## 4. v2 预留（本轮不做）

多机部署（盯 #1563）· EasyTier 外网访问 · ntfy 推送（opencode `session.idle` 插件）· OpenAI 兼容 API 后端切换演练 · Windows 原生 NSSM 部署文档
