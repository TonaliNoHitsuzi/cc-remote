# 交接说明（给下一个对话的 AI 和未来的自己）

> 本目录 = cc-remote 部署工程。方案已定稿、未开工。你（新对话 AI）的任务：按 `02-开发计划.md` 从 M0 执行。项目守则见根目录 `AGENTS.md`（会自动加载）。

## 开工三步

1. **读文档**：`README.md` → `docs/01-方案定稿.md`（已定项勿翻案）→ `docs/02-开发计划.md`（你的任务表）→ 拿不准的事实查 `docs/03-事实调研汇编.md`
2. **环境核对**（WSL：`wsl -d Ubuntu-24.04 -e bash -c '...'`）：
   ```bash
   docker --version && docker ps        # Docker 29，服务在跑
   node -v                              # 或确认用 GitHub release 二进制装 cc-connect
   opencode --version                   # 宿主机已有；容器内另装 v1.18.26
   ls ~/.config/opencode ~/.local/share/opencode   # 凭证在（挂载源）
   ```
3. **从 M0 开始**：ACP Spike 是生死验证，结论决定走 ACP 还是兜底 B/C（见 01 文档第 5 节）。M1（飞书应用创建）需要**用户手机操作**（扫码建团队/登录开发者后台），提前和用户约。

## 用户是谁 / 习惯什么

- 用户懂技术但可能不懂细节名词，重要名词要解释；遇决策点先问，不要猜
- 中文交流；Windows 侧中文/编码敏感（WSL 里干活可避开）
- 用户已逐条确认 12 条共识（01 文档第 3 节），**改任何一条都要先问**

## 外部资源

- 两份完整调研报告：`docs/research/`（副本）与 `E:\网络调研\`（原件）
- cc-connect 源码已克隆过：`C:\Users\Zzy\AppData\Local\Temp\opencode\cc-connect-src`（临时目录，可能已清，需要就重新 clone）
- 关键上游：github.com/chenhg5/cc-connect · opencode.ai/docs · open.feishu.cn/document

## 交付物去向

Dockerfile、compose、配置模板、文档全部放本目录（`~/AgentRoot/cc-remote/`），AppID/Secret 等**敏感信息走 .env 不进任何文档**。

## 完工定义

M0–M6 全绿：飞书发指令→agent 执行→审批卡往返→Dashboard 可看→重启自愈→文档成套。
